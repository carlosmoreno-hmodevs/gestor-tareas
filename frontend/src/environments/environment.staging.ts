export const environment = {
  production: false,
  gamoraApiEnabled: true,
  /** URL pública HTTPS del API en staging — ajustar al desplegar */
  gamoraApiUrl: 'https://api-staging.tu-dominio.com',
};
